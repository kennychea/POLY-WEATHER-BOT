# P5.1.1 — DB: Add dedup tracking table + query methods

You are working on a Polymarket weather arbitrage bot. Read `tasks/lessons.md` first. Read `.planning/phases/phase-5/PLAN.md` section P5.1.1 for full details.

## Task
Add a `traded_positions` table to `infra/db.py` to track which (market_id, signal_type) pairs have open positions.

## Files to modify
- `infra/db.py` — add table schema, columns, CRUD methods
- `tests/test_db.py` — add 4 new tests

## Steps
1. Read `infra/db.py` to understand existing schema pattern
2. Add `traded_positions` table to `_SCHEMAS` dict:
   - Columns: id, market_id, signal_type, trade_id, opened_at, status
   - UNIQUE constraint on (market_id, signal_type, status)
3. Add `_COLUMNS["traded_positions"]` with matching column list
4. Add `read_traded_positions(status="pending") -> list[dict]` method
5. Add `mark_position_resolved(market_id, signal_type)` method
6. Write tests: table creation, enqueue, read pending, mark resolved
7. Run `python -m pytest tests/test_db.py -v` — all must pass
8. Run `python -m pytest tests/ -v` — 0 regressions

## Constraints
- Don't modify existing table schemas
- Follow existing code patterns in db.py
- Use aiosqlite async patterns

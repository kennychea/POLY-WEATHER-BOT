# P5.2.2 — Event-based trade resolution

You are working on a Polymarket weather arbitrage bot. Read `tasks/lessons.md` first. Read `.planning/phases/phase-5/PLAN.md` section P5.2.2 for full details.

## Prerequisites
P5.2.1 must be done (MarketResolution type + check_resolution() method).
P5.1.2 must be done (dedup system — _resolve_trade must call dedup.remove()).

## Task
Replace the fake 30s time-based resolution with event-based resolution: poll Gamma for closed=true, resolve with actual market outcome, 7-day force-resolve for disappeared markets.

## Files to modify
- `simulator/paper_trader.py` — rewrite check_pending() and _resolve_trade()
- `infra/db.py` — add resolution_source column to paper_trades schema + migration
- `tests/test_resolution.py` (new) — 7 new tests

## Steps
1. Read `simulator/paper_trader.py` thoroughly (especially lines 174-371)
2. Read the COMPLETE P5.2.2 section in PLAN.md — it has the full _resolve_trade() body
3. Rewrite check_pending(): for each pending trade, call market_indexer.check_resolution()
   - If MarketResolution returned: resolve with actual price
   - If None and <7 days: keep pending
   - If None and >7 days: force-resolve
4. Rewrite _resolve_trade() with COMPLETE implementation including:
   - Exit price from resolution.resolution_price (invert for BUY_NO)
   - PnL computation with entry + exit fees
   - DB persistence with resolution_source
   - CalibrationRecord with confidence mapped: high=0.9, medium=0.5, low=0.2
   - Reconciler.analyze() call
   - Risk manager bankroll update
   - **dedup.remove()** + **db.mark_position_resolved()** — CRITICAL, don't forget!
   - Telegram alert_resolution() call
5. Update _force_resolve() similarly with dedup removal
6. Add resolution_source column migration to db.py
7. Remove delay_seconds from resolution logic (keep param for backward compat)
8. Write tests: YES wins, YES loses, BUY_NO inverts, market open stays pending, 7-day force, source recorded, calibration logged
9. Run `python -m pytest tests/test_resolution.py tests/test_db.py -v` — all pass
10. Run `python -m pytest tests/ -v` — ALL 177+ tests pass

## Constraints
- This is the MOST CRITICAL function — no shortcuts, no ellipsis
- dedup.remove() MUST be called in both _resolve_trade() and _force_resolve()
- Never resolve using signal direction — use actual resolution price
- Model both entry and exit fees

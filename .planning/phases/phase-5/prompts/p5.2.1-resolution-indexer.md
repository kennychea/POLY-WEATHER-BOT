# P5.2.1 — MarketIndexer resolution checking

You are working on a Polymarket weather arbitrage bot. Read `tasks/lessons.md` first. Read `.planning/phases/phase-5/PLAN.md` section P5.2.1 for full details.

CRITICAL LESSON: Gamma API condition_id param returns unrelated markets — always verify conditionId matches!

## Task
Add `check_resolution(condition_id)` method to MarketIndexer that checks if a market has resolved (closed=true) via cache then Gamma API fallback. Add `MarketResolution` dataclass to types.py.

## Files to modify
- `infra/types.py` — add MarketResolution dataclass (append only, don't modify existing)
- `infra/types.py` — add `resolution_source: str = ""` to WeatherPaperTrade (with default)
- `market_data/indexer.py` — add check_resolution() and _extract_resolution() methods
- `tests/test_indexer.py` (new) — 6 new tests

## Steps
1. Read `infra/types.py` and `market_data/indexer.py`
2. Add frozen MarketResolution dataclass: condition_id, resolution_price, closed, source
3. Add `resolution_source: str = ""` field at END of WeatherPaperTrade (default preserves compat)
4. Add `check_resolution(condition_id)` to MarketIndexer:
   - L1: scan cached self._markets for matching conditionId with closed=true
   - L2: targeted Gamma API call if not in cache
   - GUARD: verify returned conditionId matches requested (Gamma is unreliable!)
5. Add `_extract_resolution(market, source)` static method to parse outcomePrices
6. Write tests: cache closed, cache open, API fallback, API mismatch guard, extract valid, extract missing
7. Run `python -m pytest tests/test_indexer.py -v` — all pass
8. Run `python -m pytest tests/ -v` — 0 regressions

## Constraints
- infra/types.py is PROTECTED — only append new dataclass + add default fields
- NEVER trust Gamma condition_id param blindly — always verify conditionId in response
- Use outcomePrices for resolution price (CLOB is useless for weather)

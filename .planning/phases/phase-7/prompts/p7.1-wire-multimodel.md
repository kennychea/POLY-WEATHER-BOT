# P7.1 — Wire Multi-Model into main.py Pipeline

## Objective
Replace single-model GFS usage in `main.py:_evaluate_market()` with the multi-model pipeline already proven in `cli_scanner.py`.

## Current State
- `main.py:250` calls `fetch_ensemble_result()` → GFS only, 31 members
- `cli_scanner.py:129` calls `fetch_multi_model_result()` → GFS+ECMWF+ICON, 122 members
- `multi_model_probability()` exists and works

## Changes

### main.py
1. Add import: `fetch_multi_model_result` from `weather.ensemble`
2. Add import: `multi_model_probability` from `weather.probability`
3. In `_evaluate_market()` (around line 250):
   - Replace `fetch_ensemble_result()` call with `fetch_multi_model_result()`
   - Build `model_members` dict: `{name: list(er.members) for name, er in multi.items()}`
   - Replace `ensemble_probability()` call with `multi_model_probability()`
   - Set `total_member_count = sum(er.member_count for er in multi.values())`
4. Update log line to show model count
5. Keep `fetch_ensemble_result` import for backward compat (other code may use it)

### Reference: cli_scanner.py lines 128-149
Copy the pattern exactly.

## Tests
- Existing tests in `tests/test_resolution.py`, `tests/test_ensemble.py` must still pass
- Add test: `test_evaluate_market_calls_multi_model` — mock `fetch_multi_model_result` and verify it's called

Recherche YouTube pour : $ARGUMENTS

Execute le script de recherche YouTube avec les mots-cles fournis :

```bash
python3 .claude/skills/yt-search/scripts/yt_search.py "$ARGUMENTS"
```

Affiche les resultats tels quels. Si aucun argument n'est fourni, demande a l'utilisateur quels mots-cles rechercher.
